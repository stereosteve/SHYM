{
  "scripts": {
    "db": "wrangler d1 execute tunez-db --local --file=./schema.sql",
    "dev": "wrangler dev src/index.tsx",
    "deploy": "wrangler deploy --minify src/index.tsx"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3",
    "hono": "^3.12.6",
    "ulid-workers": "^2.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.23.0"
  }
}