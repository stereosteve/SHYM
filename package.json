{
  "scripts": {
    "db": "wrangler d1 execute my-db --local --file=./schema.sql",
    "dev": "wrangler dev src/index.tsx",
    "deploy": "wrangler deploy --minify src/index.tsx"
  },
  "dependencies": {
    "hono": "^3.12.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^3.23.0"
  }
}